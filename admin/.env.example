# Base URL of the matrimony_backend Go API (no trailing slash).
# Same backend the Flutter app talks to — see frontend/lib/core/api/api_endpoints.dart
# for the LAN IP this dev machine currently uses.
VITE_API_BASE_URL=http://localhost:58080
